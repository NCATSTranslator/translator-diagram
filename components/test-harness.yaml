# Test Harness (test-harness)
#
# One Translator component. Fields hold *pointers* to the places that already
# know about this component; see docs/component-metadata.md for the format and
# docs/metadata-sources.md for what each of those places actually offers.
#
# A field that is absent has not been recorded yet. A field that is explicitly
# `null` has been checked and does not exist.
# yaml-language-server: $schema=../schema/component.schema.json

id: test-harness
name: Test Harness
owner: DOGSLED
refactor_status: Continues into Refactor
layer: Shared services
hosted_at: RENCI

identifiers:
  helm_chart: test-harness

itrb:
  group: null

connections:
  gets_results_from: [ars]
  calls: [smartapi]
  externals:
    - direction: out
      name: Engineering

repositories:
  - url: https://github.com/TranslatorSRI/TestHarness
    role: source
    visibility: public
  - url: https://github.com/helxplatform/translator-devops/tree/develop/helm/test-harness
    role: helm-chart
    visibility: public

notes: >-
  The Test Harness uses Smart API to grab the urls for the various
  components it tests. It isn't really hosted, but is a spawned python
  script that runs on the RENCI server.
