# BioThings Pending API (pending-api)
#
# One Translator component. Fields hold *pointers* to the places that already
# know about this component; see docs/component-metadata.md for the format and
# docs/metadata-sources.md for what each of those places actually offers.
#
# A field that is absent has not been recorded yet. A field that is explicitly
# `null` has been checked and does not exist.
# yaml-language-server: $schema=../schema/component.schema.json

id: pending-api
name: BioThings Pending API
owner: DOGSURF

identifiers:
  itrb_app: BioThings PendingAPI (BTEP)
  itrb_group: Exploring-Agent

repositories:
  - url: https://github.com/biothings/pending.api
    role: source
    visibility: public

environments:
  ci:
    url: https://biothings.ci.transltr.io/
    location: ITRB

diagram:
  refactor_status: Continues into Refactor
  layer: Shared services
  hosted_at: ITRB
  ubiquitous: false
  hide: false
  gets_results_from: []
  calls: []
  externals: []

notes: >-
  Chunlei: It hosts a lot of APIs from last phase, some we have now moved to
  CoCo's own ES server (e.g. for nodenorm, annotator, nameres), some are for
  bte, which will be removed. But the pending api as our own API host will
  probably continue being live, till all APIs there are not used. I imagine
  when we eventually reach tier 2 KGs, pending API host can be used for
  hosting some of these APIs.
